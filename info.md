# User Activity Tracker

Track only **manual** interactions in Home Assistant — switches, buttons, lights, scenes — separate from automations and scripts. Stored in its own SQLite DB.

Includes sensors, REST API, a Lovelace card and a sidebar panel. Zero configuration.
